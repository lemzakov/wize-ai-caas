# Руководство по работе с таблицей default_workflow

## Обзор

Таблица `default_workflow` предназначена для хранения ссылок на шаблоны рабочих процессов по умолчанию, которые хранятся в системе контроля версий. Эта таблица позволяет организовать и управлять коллекцией предустановленных рабочих процессов, которые можно импортировать в экземпляры WIZE.

## Структура таблицы

### Описание полей

| Поле | Тип данных | Обязательное | Описание |
|------|------------|--------------|----------|
| `id` | VARCHAR(36) | Да | Уникальный идентификатор записи (UUID) |
| `name` | VARCHAR(255) | Да | Название рабочего процесса |
| `description` | TEXT | Нет | Подробное описание рабочего процесса |
| `filePath` | VARCHAR(500) | Да | Относительный путь к файлу рабочего процесса в репозитории |
| `category` | VARCHAR(100) | Нет | Категория для организации рабочих процессов |
| `tags` | TEXT | Нет | Теги для фильтрации и поиска (хранятся как список, разделенный запятыми) |
| `isActive` | BOOLEAN | Да | Определяет, доступен ли рабочий процесс для импорта (по умолчанию: true) |
| `displayOrder` | INTEGER | Да | Порядок отображения в списке (по умолчанию: 0) |
| `icon` | VARCHAR(50) | Нет | Иконка или эмодзи для визуального представления |
| `createdAt` | TIMESTAMP(3) | Да | Дата и время создания записи |
| `updatedAt` | TIMESTAMP(3) | Да | Дата и время последнего обновления записи |

### Индексы

Таблица имеет следующие индексы для оптимизации производительности:
- `IDX_default_workflow_name` - индекс по полю `name`
- `IDX_default_workflow_category` - индекс по полю `category`
- `IDX_default_workflow_isActive` - индекс по полю `isActive`

## Использование

### 1. Добавление нового рабочего процесса по умолчанию

#### Шаг 1: Подготовка файла рабочего процесса

Создайте JSON-файл с определением рабочего процесса в директории репозитория. Рекомендуемая структура:

```
/default-workflows/
  /automotive/
    subscription-onboarding.json
    customer-support.json
  /general/
    email-notification.json
```

Пример JSON-файла рабочего процесса (`subscription-onboarding.json`):

```json
{
  "name": "Онбординг подписки на автомобиль",
  "nodes": [...],
  "connections": {...},
  "settings": {...}
}
```

#### Шаг 2: Добавление записи в базу данных

**Вариант A: Прямой SQL-запрос**

```sql
INSERT INTO default_workflow (
    id,
    name,
    description,
    filePath,
    category,
    tags,
    isActive,
    displayOrder,
    icon,
    createdAt,
    updatedAt
) VALUES (
    'a1b2c3d4-e5f6-7890-abcd-ef1234567890',
    'Онбординг подписки на автомобиль',
    'Автоматизированный процесс регистрации клиента при оформлении подписки на автомобиль. Включает проверку документов, создание учетной записи и отправку приветственного письма.',
    'default-workflows/automotive/subscription-onboarding.json',
    'automotive',
    'подписка,онбординг,автомобиль,клиент',
    true,
    10,
    '🚗',
    CURRENT_TIMESTAMP(3),
    CURRENT_TIMESTAMP(3)
);
```

**Вариант B: Использование ORM (TypeORM)**

```typescript
import { DefaultWorkflow } from '@n8n/db';
import { v4 as uuidv4 } from 'uuid';

const defaultWorkflow = new DefaultWorkflow();
defaultWorkflow.id = uuidv4();
defaultWorkflow.name = 'Онбординг подписки на автомобиль';
defaultWorkflow.description = 'Автоматизированный процесс регистрации клиента...';
defaultWorkflow.filePath = 'default-workflows/automotive/subscription-onboarding.json';
defaultWorkflow.category = 'automotive';
defaultWorkflow.tags = ['подписка', 'онбординг', 'автомобиль', 'клиент'];
defaultWorkflow.isActive = true;
defaultWorkflow.displayOrder = 10;
defaultWorkflow.icon = '🚗';

await defaultWorkflow.save();
```

### 2. Обновление существующего рабочего процесса

```sql
UPDATE default_workflow
SET
    name = 'Онбординг подписки на автомобиль (обновлено)',
    description = 'Улучшенная версия процесса онбординга...',
    displayOrder = 5,
    updatedAt = CURRENT_TIMESTAMP(3)
WHERE id = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890';
```

### 3. Деактивация рабочего процесса

Вместо удаления, рекомендуется деактивировать рабочий процесс:

```sql
UPDATE default_workflow
SET
    isActive = false,
    updatedAt = CURRENT_TIMESTAMP(3)
WHERE id = 'a1b2c3d4-e5f6-7890-abcd-ef1234567890';
```

### 4. Получение списка активных рабочих процессов

```sql
SELECT
    id,
    name,
    description,
    category,
    icon,
    displayOrder
FROM default_workflow
WHERE isActive = true
ORDER BY displayOrder ASC, name ASC;
```

### 5. Фильтрация по категории

```sql
SELECT *
FROM default_workflow
WHERE
    isActive = true
    AND category = 'automotive'
ORDER BY displayOrder ASC;
```

## Рекомендуемые категории

Для проекта WIZE рекомендуется использовать следующие категории:

- `automotive` - Рабочие процессы, связанные с автомобильной подпиской
- `customer-service` - Процессы обслуживания клиентов
- `subscription-management` - Управление подписками
- `notification` - Уведомления и коммуникации
- `payment` - Платежные процессы
- `analytics` - Аналитика и отчетность
- `general` - Общие вспомогательные процессы

## Примеры типичных рабочих процессов для WIZE

### 1. Регистрация новой подписки

```sql
INSERT INTO default_workflow (
    id, name, description, filePath, category, tags, isActive, displayOrder, icon
) VALUES (
    gen_random_uuid(),
    'Регистрация новой подписки',
    'Полный цикл регистрации клиента на автомобильную подписку',
    'default-workflows/automotive/new-subscription.json',
    'automotive',
    'подписка,регистрация,новый клиент',
    true,
    1,
    '📝'
);
```

### 2. Обработка окончания подписки

```sql
INSERT INTO default_workflow (
    id, name, description, filePath, category, tags, isActive, displayOrder, icon
) VALUES (
    gen_random_uuid(),
    'Обработка окончания подписки',
    'Автоматизация процесса завершения подписки и возврата автомобиля',
    'default-workflows/subscription-management/subscription-end.json',
    'subscription-management',
    'подписка,завершение,возврат',
    true,
    2,
    '🔚'
);
```

### 3. Напоминание о продлении

```sql
INSERT INTO default_workflow (
    id, name, description, filePath, category, tags, isActive, displayOrder, icon
) VALUES (
    gen_random_uuid(),
    'Напоминание о продлении подписки',
    'Автоматические напоминания клиентам за 30, 14 и 7 дней до окончания подписки',
    'default-workflows/notification/renewal-reminder.json',
    'notification',
    'напоминание,продление,уведомление',
    true,
    3,
    '⏰'
);
```

## Миграция данных

### Создание таблицы

Таблица автоматически создается при запуске миграций базы данных:

```bash
# Для PostgreSQL
pnpm migration:run:postgres

# Для MySQL
pnpm migration:run:mysql

# Для SQLite
pnpm migration:run:sqlite
```

### Откат миграции

Если необходимо откатить создание таблицы:

```bash
pnpm migration:revert
```

## Лучшие практики

1. **Именование файлов**: Используйте осмысленные названия для файлов рабочих процессов (например, `customer-onboarding.json`, а не `workflow1.json`)

2. **Организация директорий**: Группируйте файлы рабочих процессов по категориям в отдельных поддиректориях

3. **Описания**: Всегда заполняйте поле `description` подробной информацией о назначении и функциях рабочего процесса

4. **Теги**: Используйте релевантные теги для облегчения поиска и фильтрации

5. **Порядок отображения**: Устанавливайте `displayOrder` так, чтобы наиболее важные и часто используемые процессы отображались первыми

6. **Версионирование**: При обновлении файла рабочего процесса обновляйте поле `updatedAt` в базе данных

7. **Резервное копирование**: Регулярно создавайте резервные копии как таблицы базы данных, так и файлов рабочих процессов

## Устранение неполадок

### Проблема: Рабочий процесс не отображается

**Решение**: Проверьте, что поле `isActive` установлено в `true`:

```sql
SELECT id, name, isActive
FROM default_workflow
WHERE name LIKE '%название%';
```

### Проблема: Файл рабочего процесса не найден

**Решение**: Убедитесь, что путь в поле `filePath` указан правильно и файл существует в репозитории:

```sql
SELECT id, name, filePath
FROM default_workflow
WHERE id = 'your-workflow-id';
```

### Проблема: Дубликаты рабочих процессов

**Решение**: Проверьте наличие дубликатов по названию:

```sql
SELECT name, COUNT(*)
FROM default_workflow
GROUP BY name
HAVING COUNT(*) > 1;
```

## Поддержка

Для получения дополнительной помощи или сообщения о проблемах, пожалуйста, свяжитесь с командой разработки WIZE.

---

**Версия документа**: 1.0  
**Дата последнего обновления**: Январь 2026  
**Платформа**: WIZE - Платформа по цифровизации и автоматизации взаимодействия с клиентом в рамках жизненного цикла подписки на автомобиль
